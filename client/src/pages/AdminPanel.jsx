/*
 * Assigned Member: Frontend Member 2 (Web Dashboards)
 * Required Functions: Admin Dashboard (Table of pending apps with Approve/Reject buttons)
 */
import React from 'react';

const AdminPanel = () => {
  // Needs JWT token from Backend Member 1 to restrict access
  return (
    <div>
      <h1>Admin Dashboard</h1>
      {/* Table of pending apps */}
      {/* Approve/Reject Buttons */}
    </div>
  );
};

export default AdminPanel;
